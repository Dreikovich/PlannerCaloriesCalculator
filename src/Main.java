
public class Main {
    public static void main(String[] args) {
   /*     DataManager dataManager = new DataManager();
        String foodData = dataManager.readFromFile("data/food.txt");*/
        LoginGUI loginGUI = new LoginGUI();

        /*System.out.println("Food Data from foods.txt:");
        System.out.println(foodData);
        //Calories, Fats(g):Carbs(g):Protein(g):Fiber(g):Sugar(g):Sodium(

        Planner planner = new Planner();

        // Creating a Meal object
        Food food1 = new Food("Chicken Salad", 350, 12.5, 15, 30, 5, 2, 450);
        Food food2 = new Food("Chicken", 350, 12.5, 15, 30, 5, 2, 450);
        String mealData = DataHelper.formatMealData(food1);
        //DataManager.writeToFile("data/food.txt", mealData);

        Meal meal = new Meal();
        meal.addFoodToMeal(food1);
        meal.addFoodToMeal(food2);

        planner.addMeal(meal);

        // Displaying planned meals
        planner.displayPlannedMeals();

        DataManager.writeToFile("data/food.txt", mealData);*/
    }
}